import { WorkoutPlanFilterPipe } from './workout-plan-filter.pipe';

describe('WorkoutPlanFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkoutPlanFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
