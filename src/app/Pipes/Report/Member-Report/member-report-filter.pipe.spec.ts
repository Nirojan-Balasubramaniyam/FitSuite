import { MemberReportFilterPipe } from './member-report-filter.pipe';

describe('MemberReportFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberReportFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
