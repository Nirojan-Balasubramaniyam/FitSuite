import { ProgramTypeFilterPipe } from './program-type-filter.pipe';

describe('ProgramTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
